global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: pix-nfc-app
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["host.docker.internal:8081"]
        labels:
          app: pix-nfc-reference
          env: local

  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:8889"]
        labels:
          app: otel-collector
